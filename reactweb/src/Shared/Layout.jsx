function Layout({ children }) {

    return (
        <div>
            <header>LAYOUT</header>
            <main>{children}</main>
            <footer>LAYOUT</footer>
        </div>
    )
}

export default Layout;